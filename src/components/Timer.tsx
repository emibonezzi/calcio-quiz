import { Box } from "@chakra-ui/react";
import React from "react";

const Timer = () => {
  return (
    <Box fontFamily="courier" fontWeight="100">
      00:30
    </Box>
  );
};

export default Timer;
